[workspace]
resolver = "3"
members = ["core", "display"]

[profile.release]
opt-level = 3
debug = false
lto = "thin"
codegen-units = 1

# Use opt-level=1 for dev to avoid wgpu shader validation crashes
[profile.dev]
opt-level = 1
debug = true
debug-assertions = true
overflow-checks = false
